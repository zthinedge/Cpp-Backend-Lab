# 线程池
## 1. 定义与核心功能
线程池是一种**维持**和**管理固定数量线程**的池式结构，用于高效处理异步任务。

* **维持**：预先创建线程，避免频繁创建/销毁线程带来的内核态切换开销。
* **管理**：监控线程状态。有任务时指派执行，无任务时进入休眠状态以释放 CPU 资源。

---
## 2. 线程池的组成
线程池的核心架构遵循 **生产者-消费者** 模型：

1.  **工作线程 (workers_)**：固定数目的线程，充当消费者。
2.  **任务队列 (tasks_)**：存放待处理任务的缓冲区，充当媒介。
3.  **同步机制**：通常使用 `std::mutex`和 `std::condition_variable`。

--- 

## 3. 核心流程分析

### 线程池的构造
1.  **预热**：创建 $n$ 个工作线程，启动后立即通过条件变量进入休眠等待状态。
2.  **任务分发**：当新任务加入队列，调用 `notify_one()` 或 `notify_all()` 唤醒线程。
3.  **竞争执行**：唤醒的线程竞争互斥锁，成功者从队列取走任务并执行。
4.  **安全停止**：收到停止指令后，停止接收新任务，待队列中剩余任务执行完毕后，线程正常退出。

### 添加任务
1.  **加锁**：获取队列的互斥锁，保证线程安全。
2.  **状态检查**：确认线程池未处于停止状态。
3.  **入队**：将任务封装并推入队列。
4.  **通知**：唤醒等待中的工作线程。

### 线程池的析构
1.  **设置标志位**：将 `stopping` 终止变量设置为 `true`。
2.  **全员唤醒**：调用 `notify_all()`，防止线程死锁在等待状态。
3.  **回收资源**：遍历线程容器，调用 `join()` 阻塞等待所有线程安全结束。

---

## 4. 编译测试
进入当前目录后执行：

```bash
g++ -std=c++17 -Wall -Wextra -pedantic -pthread ThreadPool.cpp test_threadpool.cpp -o threadpool_test
./threadpool_test
```

如果测试通过，会输出：

```text
All ThreadPool tests passed.
```

